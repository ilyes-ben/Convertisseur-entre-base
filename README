Le but du programme est de convertire un nombre de base n aux base m par (max base 10) , le programe peut detect si un nombre est vraiment dans la base entrer par l'utilisateur ou non . pour plus de detail ouvrire le convertisseur.c qui contien des commentaires pour expilquer comment le code fonction
les fichiers sont :
convertisseur.c  (le fichier du code)
convertisseur (l'executable dans le terminal)
convertisseur.cbp (le fichier du code blocks)
